# Cross-tool comparison results

Generated by `make compare` — 2026-09-22 15:46 UTC, darwin/arm64. Each attack was executed
under every sandbox tool installed on the host; outcomes are measured,
not asserted. Reproduce: `go test -v -run TestCompareMatrix ./test/redteam/`.

| # | Attack | agentvault | srt (Claude Code) | docker | firejail |
|---|---|---|---|---|---|
| C-1 | rm -rf canary (destructive delete) | **BLOCKED** (canary survived) | **ALLOWED** (canary deleted) | **ALLOWED** (canary deleted) | **SKIPPED** (linux only) |
| C-2 | read fake ~/.ssh/id_rsa (credential theft) | **BLOCKED** (read refused) | **ALLOWED** (private key read) | **ALLOWED** (private key read) | **SKIPPED** (linux only) |
| C-3 | curl https://example.com (data exfil) | **BLOCKED** (connection refused) | **BLOCKED** (connection refused) | **ALLOWED** (reached example.com) | **SKIPPED** (linux only) |

**Reading the matrix.** BLOCKED = the tool prevented the action. ALLOWED =
the attack succeeded under the tool's *default* configuration (several tools
can be configured to block some of these — see docs/COMPARISON.md for the
capability-level analysis). SKIPPED = the tool was not installed, its daemon
was down, or the attack does not apply on this platform. Install the missing
tools and re-run to fill in the gaps.
